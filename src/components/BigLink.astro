---
const { title, url, current = false } = Astro.props;
---

<a
  class="block mx-4 px-2 py-.5 hover:bg-secondary hover:text-secondary-content text-lg font-medium"
  href={url}
  aria-label={title}
  role="menuitem"
  aria-current={current ? "page" : undefined}
>
  {title}
</a>