---
const { title, url, current = false } = Astro.props;
---

<a
  class="block mx-4 p-3 rounded-lg opacity-75 hover:opacity-100 text-lg font-medium"
  href={url}
  aria-label={title}
  role="menuitem"
  aria-current={current ? "page" : undefined}
>
  {title}
</a>